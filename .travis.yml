language: python
python:
  - "3.6"
install:
  - pip install mock --use-mirrors
  - pip install Django==1.6.11 --use-mirrors
  - pip install git+git://github.com/gears/gears.git --use-mirrors
  - pip install . --use-mirrors
script: make test
